name: "Zip Reimu"

on:
  push:
    paths:
      - "HakureiReimu/**"

jobs:
  zip-files:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: vimtor/action-zip@v1
        with:
          files: HongMeiling/
          dest: EXPORT/Meiling/HakureiReimu.zip
          
      - name: "retroactively add latest zip meiling"
        uses: stefanzweifel/git-auto-commit-action@v4  